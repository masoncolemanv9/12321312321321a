__PLACEHOLDER__ — This action requires fetching the existing file contents from GitHub and applying a targeted in-place replacement of the `cb_dl_edit` function. The available tool here overwrites the entire file, but the parent guidelines explicitly require keeping all other ~2,600 lines byte-for-byte identical. Without the ability to fetch the current file contents within this tool, executing a full overwrite would destroy the rest of the file. Aborting safe execution; parent agent should use a GitHub Contents API tool (GET then PUT with sha) instead of this overwrite action.