# Render Blueprint — https://render.com/docs/infrastructure-as-code
#
# Generated by scripts/gen_render_yaml.py — DO NOT hand-edit individual
# service blocks. Edit bot/persona.py to add/rename personas, then
# regenerate:
#
#     python scripts/gen_render_yaml.py --first 5    # boss + 4 leads
#     python scripts/gen_render_yaml.py              # full 20-bot farm
#
# Usage on Render:
#   1. https://dashboard.render.com → New + → Blueprint
#   2. Connect this GitHub repo
#   3. Render reads this file and asks for each service's BOT_TOKEN
#      — leave it EMPTY for any bot you haven't created in BotFather
#      yet. Those services boot in dormant mode (only /healthz).
#   4. Apply → each service auto-deploys with its own 1GB persistent disk
#   5. /start each bot in Telegram → claim ownership → /setup → API key
#   6. To activate a dormant bot later: Render dashboard → service →
#      Environment → set BOT_TOKEN → save (service auto-restarts)

services:

  # ===== 1. Lilush Boss =================================================
  - type: web
    name: lilush-boss
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: boss
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-boss-data
      mountPath: /data
      sizeGB: 1

  # ===== 2. Research Lead =================================================
  - type: web
    name: lilush-research-lead
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: research_lead
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-research-lead-data
      mountPath: /data
      sizeGB: 1

  # ===== 3. Debate Lead =================================================
  - type: web
    name: lilush-debate-lead
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: debate_lead
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-debate-lead-data
      mountPath: /data
      sizeGB: 1

  # ===== 4. Coder Lead =================================================
  - type: web
    name: lilush-coder-lead
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: coder_lead
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-coder-lead-data
      mountPath: /data
      sizeGB: 1

  # ===== 5. DevOps Lead =================================================
  - type: web
    name: lilush-devops-lead
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: devops_lead
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-devops-lead-data
      mountPath: /data
      sizeGB: 1

  # ===== 6. GitHub Scout =================================================
  - type: web
    name: lilush-github-scout
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: github_scout
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-github-scout-data
      mountPath: /data
      sizeGB: 1

  # ===== 7. Reddit Scout =================================================
  - type: web
    name: lilush-reddit-scout
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: reddit_scout
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-reddit-scout-data
      mountPath: /data
      sizeGB: 1

  # ===== 8. HN Scout =================================================
  - type: web
    name: lilush-hn-scout
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: hn_scout
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-hn-scout-data
      mountPath: /data
      sizeGB: 1

  # ===== 9. Apify Runner =================================================
  - type: web
    name: lilush-apify-runner
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: apify_runner
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-apify-runner-data
      mountPath: /data
      sizeGB: 1

  # ===== 10. D1 Skeptic =================================================
  - type: web
    name: lilush-d1-skeptic
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: d1_skeptic
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-d1-skeptic-data
      mountPath: /data
      sizeGB: 1

  # ===== 11. D2 Optimist =================================================
  - type: web
    name: lilush-d2-optimist
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: d2_optimist
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-d2-optimist-data
      mountPath: /data
      sizeGB: 1

  # ===== 12. Judge =================================================
  - type: web
    name: lilush-judge
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: judge
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-judge-data
      mountPath: /data
      sizeGB: 1

  # ===== 13. TZ Writer =================================================
  - type: web
    name: lilush-tz-writer
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: tz_writer
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-tz-writer-data
      mountPath: /data
      sizeGB: 1

  # ===== 14. Devin Spawner =================================================
  - type: web
    name: lilush-devin-spawner
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: devin_spawner
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-devin-spawner-data
      mountPath: /data
      sizeGB: 1

  # ===== 15. PR Reviewer =================================================
  - type: web
    name: lilush-pr-reviewer
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: pr_reviewer
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-pr-reviewer-data
      mountPath: /data
      sizeGB: 1

  # ===== 16. Tester =================================================
  - type: web
    name: lilush-tester
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: tester
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-tester-data
      mountPath: /data
      sizeGB: 1

  # ===== 17. Watchdog =================================================
  - type: web
    name: lilush-watchdog
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: watchdog
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-watchdog-data
      mountPath: /data
      sizeGB: 1

  # ===== 18. Render Admin =================================================
  - type: web
    name: lilush-render-admin
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: render_admin
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-render-admin-data
      mountPath: /data
      sizeGB: 1

  # ===== 19. GitHub Admin =================================================
  - type: web
    name: lilush-github-admin
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: github_admin
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-github-admin-data
      mountPath: /data
      sizeGB: 1

  # ===== 20. Archivist =================================================
  - type: web
    name: lilush-archivist
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_PERSONA
        value: archivist
      - key: BOT_TOKEN
        sync: false        # set in Render dashboard
      - key: BOT_MODE
        value: polling
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /data
      - key: ALLOWED_USER_IDS
        sync: false
    disk:
      name: lilush-archivist-data
      mountPath: /data
      sizeGB: 1
